# 积分商品通用化（V2.9）实施计划【最终版】

## Context

离线游戏安装包的价格（99积分）、划线价（199）、下载链接、有效期（365天）硬编码散落在 2 个文件 9 处，近期已因调价/换链接各变更过一次，存在"展示价与扣费价不一致"的资损风险。后续还会上同类可变现商品，做通用方案：

- 新建 `point_goods` 商品表 + 管理端 CRUD，离线包为首条记录
- `offline_game_order` **RENAME 为 `point_goods_order` + 加 `product_key`**（用户确认：生产环境暂无订单，迁移零风险）
- 兑换 API 通用骨架（`point_goods_redeem.php`），按 `product_key` 路由履约（当前实现 license 类）
- 前端（more.php / course_my.php / point_index.php）改读商品配置
- **不做**：课程交易并入、营销页面模板化、购物车

## 一、SQL 归档：db/V2.9_20260909_point_goods.sql（新建）

```sql
-- V2.9: 积分商品通用化
-- 1) 新建 point_goods 商品表，离线游戏离线包为首条商品记录
-- 2) offline_game_order 通用化：加 product_key 列后重命名为 point_goods_order
-- 3) 历史离线游戏流水 point_log type 4→6（积分商品），使流水页正确归类

-- ============ 1. 商品表 ============
CREATE TABLE IF NOT EXISTS `point_goods` (
  `id` INT AUTO_INCREMENT PRIMARY KEY COMMENT '自增ID',
  `product_key` VARCHAR(32) NOT NULL COMMENT '商品唯一标识（履约路由键，如 offline_game）',
  `title` VARCHAR(100) NOT NULL COMMENT '商品名称',
  `description` VARCHAR(500) NOT NULL DEFAULT '' COMMENT '商品描述（前端横幅/弹窗展示）',
  `icon` VARCHAR(16) NOT NULL DEFAULT '' COMMENT '图标（emoji，可为空）',
  `price` INT NOT NULL DEFAULT 0 COMMENT '兑换价格（积分）',
  `original_price` INT NULL DEFAULT NULL COMMENT '划线价（积分，NULL=不展示划线价）',
  `download_url` VARCHAR(500) NOT NULL DEFAULT '' COMMENT '下载链接',
  `validity_days` INT NOT NULL DEFAULT 365 COMMENT '有效期（天）',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1=上架 0=下架',
  `sort` INT NOT NULL DEFAULT 0 COMMENT '排序权重（小的在前）',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY `uk_product_key` (`product_key`),
  KEY `idx_status_sort` (`status`, `sort`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分商品表';

-- 首条商品：值迁移自硬编码。INSERT IGNORE：重跑不覆盖管理员已改配置
INSERT IGNORE INTO `point_goods`
  (`product_key`, `title`, `description`, `icon`, `price`, `original_price`,
   `download_url`, `validity_days`, `status`, `sort`)
VALUES
  ('offline_game', '课前游戏集合 · 离线安装包',
   '机房没网也能玩！包含全部17款教育游戏的离线版本，适合无网络的教学环境',
   '📦', 99, 199,
   'https://pan.baidu.com/s/1myPtSsTkkTfrAr5QgnIWsQ?pwd=wzdf',
   365, 1, 0);

-- ============ 2. 订单表通用化 ============
-- ADD COLUMN 带默认值：存量行（仅测试环境测试单）自动回填
ALTER TABLE `offline_game_order`
  ADD COLUMN `product_key` VARCHAR(32) NOT NULL DEFAULT 'offline_game' COMMENT '商品标识（关联 point_goods.product_key）' AFTER `user_id`,
  ADD INDEX `idx_product_key` (`product_key`);

RENAME TABLE `offline_game_order` TO `point_goods_order`;

-- ============ 3. 历史流水类型对齐 ============
UPDATE `point_log` SET `type` = 6
 WHERE `type` = 4 AND `change_point` < 0 AND `relation_id` LIKE 'OG%';

-- ============ 回滚SQL（逆序执行） ============
-- RENAME TABLE `point_goods_order` TO `offline_game_order`;
-- ALTER TABLE `offline_game_order` DROP INDEX `idx_product_key`, DROP COLUMN `product_key`;
-- UPDATE `point_log` SET `type` = 4 WHERE `type` = 6 AND `change_point` < 0 AND `relation_id` LIKE 'OG%';
-- DELETE FROM `point_goods` WHERE `product_key` = 'offline_game';
-- DROP TABLE IF EXISTS `point_goods`;
```

同步更新 `db/RELEASE_STEPS.md` 四处：版本历史加行 + 当前版本改 V2.9（2026-09-09）；Step 1 执行清单追加 mysql 命令；验证 SQL 块追加（SHOW TABLES LIKE 'point_goods%' 应两张 / DESCRIBE point_goods / point_goods_order.product_key 预期 varchar(32) DEFAULT 'offline_game' / SELECT point_goods 应 1 行 / 存量核对 COUNT WHERE product_key<>'offline_game' 应 0）；文末追加 V2.9 发布文件清单小节（照 V2.8 格式）。

## 二、代码改动清单

### 1. include/my_func.inc.php（修改）
- :1110 后新增 `if (!defined('POINT_LOG_TYPE_GOODS')) define('POINT_LOG_TYPE_GOODS', 6);`，更新 :1092 区段注释类型清单
- 新增助手 `point_get_goods($product_key, $only_on_sale = true)`：按 key 查 point_goods，返回行数组或 false（redeem API / more.php 模板复用）

### 2. trunk/web/point_goods_redeem.php（新建，~260 行，母本 offline_game_redeem.php）
处理流程（括号=复用旧行号）：
1. require 三件套 + header JSON（:7-9,:16）；登录/仅POST/CSRF（:19-34）
2. 读 `product_key`（新增，trim）+ school/room（:36-38）；限流10秒，key 改 `_goods_rate_`（:40-46）
3. product_key 白名单校验 `^[a-z0-9_]{1,32}$`；查 point_goods，空或 status!=1 → 统一"商品不存在或已下架"（防枚举）
4. 履约路由表 `$routes = ['offline_game' => ['order_prefix'=>'OG', 'need_school_room'=>true]]`；未注册 → "该商品暂不支持在线兑换"
5. need_school_room 时校验 school/room 非空+mb_strlen≤100（:48-57，条件化）
6. price/validity_days/download_url 取商品行（替代 :60-61、:234 硬编码）
7. 订单号 = 路由前缀+time()+random_int(1000,9999)（:64）；expire_date（:67）
8. 事务（:70-243 骨架照搬）：
   - tx_begin → point_lock_user（:73-79）
   - 事务内防重 SELECT FOR UPDATE **加 `AND product_key = ?`**（:83-97）
   - 余额不足 code -3（:99-106）
   - `point_apply_change(-price, POINT_LOG_TYPE_GOODS, $order_no, '积分商品兑换：'.title...)`（:108-121）
   - 履约 `switch($product_key)`：case 'offline_game' = python 授权码逻辑原样（:123-193：脚本/私钥检查、escapeshellarg、tempnam+shutdown清理、python3失败重试python、license JSON 校验）；default 防御性回滚
   - INSERT `point_goods_order` 加 product_key 列，WHERE NOT EXISTS 同用户同商品未过期，rowCount 判防重（:195-221）；非 license 路由 school/room/license 写 ''
   - commit（:223）
9. 响应 data：license_code/school/room/expire_date/download_url(商品行)/balance/product_key/title

### 3. 删除 trunk/web/offline_game_redeem.php（唯一调用方 more.php:1863 同批改指向）

### 4. trunk/web/template/syzoj/more.php（修改）
头部（:3-39）：
- 删 :7 og_price、:33 硬编码 URL；新增 `$og_goods = point_get_goods('offline_game')`，if 内派生 $og_price/$og_original_price/$og_title/$og_desc/$og_validity_days/$og_download_url + `$og_validity_text = (==365?'一年':N.'天')` + `$og_validity_unit`
- :17-31 查询表名改 `point_goods_order` + `AND product_key='offline_game'`，整段移入 if ($og_goods)
模板体：
- 横幅 :1144-1186、兑换弹窗 :1639-1778、og 脚本块 :1780-1929 各自包 `<?php if ($og_goods): ?>`（下架→不渲染，避免变量未定义）
- :1161 标题←htmlentities($og_title)；:1166 `✅ <?php echo $og_validity_text; ?>有效期`；:1169 描述←$og_desc
- :1173-1176 划线价 span 仅 original_price 非空且>0 输出；价格 intval($og_price)；`积分/<?php echo $og_validity_unit; ?>`
- :1662 弹窗标题、:1663 副标题 `N积分兑换 · X有效期`、:1715 费用、:1728 按钮文案动态
- :1785 ogPrice、:1788 ogDownloadUrl 取商品行
- :1863 fetch 改 `point_goods_redeem.php` + FormData 加 `product_key`
- :1889/:1896 按钮重置文案用 `var ogBtnText = <?php echo json_encode('📦 确认兑换（'.intval($og_price).'积分）', JSON_HEX_TAG|JSON_HEX_AMP|JSON_HEX_APOS|JSON_HEX_QUOT); ?>;`

### 5. course_my.php + template/syzoj/course_my.php（修改）
- 控制器 :40-44：表名 point_goods_order，SELECT 加 product_key；新增 $view_goods_titles 映射（查 point_goods 全表 key→title + 兜底 `'offline_game'=>'离线游戏安装包'`）
- 模板 :142/:146 区块标题改"积分商品订单"；:159-163 行首显示商品名（htmlentities）、school·机房 span 包空串判断
- 详情弹窗 JS :528-537 school/room 字段按空隐藏；:495-496 授权码区按空隐藏

### 6. point_index.php + template/syzoj/point_index.php（修改）
- 控制器 :40-46 valid_types 加 POINT_LOG_TYPE_GOODS；:69-84 流水 SQL 照 course JOIN 追加：
  `LEFT JOIN point_goods_order pgo ON pl.type=6 AND pl.relation_id=pgo.order_no AND pgo.user_id=pl.user_id` + `LEFT JOIN point_goods pg ON pg.product_key=pgo.product_key` + SELECT 列 `pg.title AS goods_title`
- 模板：筛选下拉加"积分商品"（:89-94）；type_map（:111-117）；详情列 :147 后加商品分支（goods_title 为 NULL 兜底显示订单号）

### 7. 管理端（新增2 + 修改2）
- **admin/point_goods_list.php**（新建，母本 point_card_list.php + news_list.php 页内动作）：ob_start + admin-header + set_get_key + administrator 检查；GET `op=toggle`（check_get_key）翻转 status → PRG；销量统计 `SELECT product_key,COUNT(*),SUM(point_amount) FROM point_goods_order GROUP BY product_key`；列表 SELECT * ORDER BY status DESC,sort ASC；列：图标/名称/key/价格/划线价(NULL→'-')/有效期/排序/销量/状态/操作；非路由注册 key 标"未接入履约"；输出全 htmlentities；末尾 admin-footer.php
- **admin/point_goods_edit.php**（新建，新增/编辑合一 POST 自处理）：admin-header + set_post_key + administrator；POST 走 check_post_key；校验 title≤100/price 正整数/original_price 空或正整数/validity_days 1..3650/download_url≤500/sort 整数/status 0|1；新增模式 product_key 必填 `^[a-z0-9_]{1,32}$`，编辑模式不接收 POST 值（只读防改坏关联）；参数化 INSERT/UPDATE；duplicate key → PRG 回编辑页；成功 PRG 回列表；末尾 admin-footer.php
- **admin/menu2.php** :272 追加"积分商品管理"→ point_goods_list.php
- **admin/point_log_list.php** :19 valid_types + :42-46 type_map 加 type 6"积分商品"

### 8. db/clearup_zezhang_game_orders.sh（修改，测试工具）
表名 11 处 offline_game_order → point_goods_order；:79 退积分硬编码 50 改按订单 point_amount 逐条退；type 4→6

## 三、部署（web-2204，/home/judge/src/web/）

顺序：**SQL 先行，代码原子切换**（新代码查新表名，SQL 先行窗口内旧代码失效模式均非破坏性：横幅走未兑换分支/订单空/兑换失败提示；生产无订单，风险极小）

```
① 全部文件传到 /tmp/deploy_v29/（按目录分层，不生效）
② 虚机内 php -l 校验全部 PHP 文件（含模板）
③ mysql 执行 V2.9 SQL + 验证：
   SHOW TABLES LIKE 'point_goods%';                     -- 两张
   DESCRIBE jol.point_goods_order product_key;          -- varchar(32) DEFAULT 'offline_game'
   SELECT product_key,price,original_price,validity_days,status FROM point_goods;  -- 1 行 offline_game/99/199/365/1
④ 一批 sudo cp 原子落盘：
   point_goods_redeem.php、course_my.php、point_index.php → web/
   my_func.inc.php → include/；more.php、course_my.php、point_index.php(模板) → template/syzoj/
   point_goods_list.php、point_goods_edit.php、menu2.php、point_log_list.php → admin/
   rm /home/judge/src/web/offline_game_redeem.php
⑤ sudo php -r 'opcache_reset();'
```

回滚：git 还原代码 + SQL 尾部回滚段（逆序）。

## 四、端到端验证

1. 管理端：菜单出现"积分商品管理"，列表 1 行（📦/99/199/365/上架），销量与 SQL 统计一致
2. 改价 99→80 → more.php 横幅/弹窗/按钮显示 80（≤30秒陈旧）→ 改回 99
3. product_key 编辑页置灰；伪造 POST 不生效
4. 兑换全流程：199划线+99+"一年有效期" → 填学校/机房 → 成功授权码+下载链接（=商品行URL）→ 余额 -99
5. 重复兑换 → code -2"已拥有授权"；低余额按钮禁用；绕前端直 POST → code -3
6. point_index：type=6 行显示商品标题+OG订单号；筛选"积分商品"可用；存量流水归类正确
7. course_my："积分商品订单"区块新旧订单正常（含详情弹窗）
8. 下架 → 横幅+弹窗消失（≤30s），免费游戏列表不受影响；直 POST point_goods_redeem.php → "商品不存在或已下架"
9. 旧入口 /offline_game_redeem.php → 404；grep 无残留引用
10. admin/point_log_list.php 能筛"积分商品"
11. clearup 脚本查询段指向新表正常

## 五、关键风险

| 风险 | 对策 |
|---|---|
| RENAME 后旧代码查询失效窗口 | SQL 与代码同窗口部署（分钟级）；失效均非破坏性；生产无订单 |
| 管理端新增未注册履约的商品 | 兑换返回"暂不支持在线兑换"，列表标注"未接入履约" |
| 改价并发竞态 | 订单价格快照，"以下单时价格为准" |
| postkey 一次性令牌 | 编辑页成功 PRG；失败重绘需重新 set_post_key（禁 require_once 复用） |
| more.php $cache_time=30 | 改配置后 ≤30s 陈旧，验证时等待 |

## 实施顺序

SQL 归档 → my_func 常量+助手 → point_goods_redeem.php → more.php 模板（配对）→ course_my 两文件 → point_index 两文件 → 管理端 4 文件 → clearup 脚本 + RELEASE_STEPS.md → 删旧 API → 部署验证
